name: news
description: A Flutter Repository( ApiProvider & DbProvider ) project.


publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  sqflite: ^2.3.0
  path_provider: ^2.1.1
  equatable: ^2.0.5
  http: ^1.1.0
  ansicolor: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^2.0.0

flutter:
  
  uses-material-design: true
  